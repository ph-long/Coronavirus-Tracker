/* Additional dependicies installed: axios, react-chartjs-2, react-countup, classnames to package.json
"npm install --save axios react-chartjs-2 react-countup classnames" in bash */

import React from "react";
import ReactDOM from "react-dom";

import App from './App';

ReactDOM.render(<App/>, document.getElementById('root'))